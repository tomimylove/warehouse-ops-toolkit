# Copy to .env and fill in for local development.
# Azure SQL free-tier connection string, e.g.:
# sqlserver://<server>.database.windows.net:1433;database=<db>;user=<user>;password=<password>;encrypt=true
DATABASE_URL="sqlserver://localhost:1433;database=warehouse_ops;user=sa;password=changeme;trustServerCertificate=true"
PORT=3000
